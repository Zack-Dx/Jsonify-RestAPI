version: '3.9'
services:
  jsonify:
    build:
      context: .
      dockerfile: docker/Dockerfile
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./:/usr/src/app
      - /usr/src/app/node_modules
    env_file:
      - ./.env
